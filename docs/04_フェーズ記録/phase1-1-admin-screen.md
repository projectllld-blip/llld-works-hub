# phase1-1-admin-screen

## Phase
v1.1 簡易管理者画面

## 状態
v1.1b mock簡易管理者画面まで完了。

## 前提
- v1.0 アカウント別クラウド基盤MVP完成は完了済み。
- A0.6 PR安全分類・docs-only自動マージ設計は完了済み。
- v1.1では、いきなり本物の企業横断管理画面へ進まず、まずmockで確認項目を整理する。

## v1.1で確認したい項目
- 登録企業一覧。
- 企業名。
- 登録日。
- 最終更新日。
- 利用アプリ数。
- `app_instances` のイメージ。
- `works_portal` 有無。
- `portal_state` 有無。
- `app_data` 件数。
- 最終保存日時。
- 異常状態。

## v1.1b mock簡易管理者画面
`admin.html` を、企業管理用のmock画面として整理した。

表示するもの:
- `LLLD Works Admin`。
- `簡易管理者画面 mock` の位置づけ。
- 実データではないこと。
- 企業横断データ取得は未実装であること。
- 登録企業数、利用アプリ数、`app_data`件数、異常状態件数、最終更新日時。
- mock企業一覧。
- 選択企業の詳細mock。
- 利用アプリ一覧mock。
- 異常状態mock。
- 本物管理画面へ進む前の判断事項。

## 実装上の制限
- 実DB接続なし。
- 企業横断取得なし。
- Supabase接続なし。
- 管理用秘密キー未使用。
- Supabase Dashboard操作なし。
- DB / RLS / migration変更なし。
- 実企業データ取得なし。
- `app_data`本文表示なし。
- `portal_state`本文表示なし。
- Auth user詳細表示なし。
- 実メールアドレス表示なし。
- 個人情報表示なし。
- 決済・販売機能実装なし。

## 表示しないもの
- 実DBデータ。
- `app_data`本文。
- `portal_state`本文。
- Auth user詳細。
- 実メールアドレス。
- 個人情報。
- 決済情報。
- SeatFlow詳細データ。
- 管理用秘密キーが必要な情報。

## 本物管理者画面へ進む前の判断事項
- 管理者認証をどこで判定するか。
- 企業横断取得を許可するか。
- 管理者権限をDBのどこに持つか。
- RLSをどう設計するか。
- Edge Functionsや管理者専用APIが必要か。
- `app_data`本文を管理者に見せる必要があるか。
- 個人情報や決済情報を扱う必要があるか。

## 次の候補
v1.2 企業情報編集。

ただし、本物の企業横断管理者画面へ進む場合は、DB / RLS / 管理者認証 / 管理者APIの設計を先に行う。
