# phase1-3-app-add-request

## Phase
v1.3 アプリ追加申請

## 状態
v1.3a アプリ追加申請UI mock完了。

今回はDB保存なし。`account.html` 上で未利用アプリを確認し、画面内状態だけで「申請を受け付けました」と表示する。

PR #21 main反映後の人間ブラウザ確認で、`account.html` 上の「アプリ追加申請」エリアが見つけにくく、既存問い合わせフォームの準備中文言と混同しやすいことが分かった。

追加修正として、利用中アプリ一覧の近くにある申請エリアを強調表示し、heroから `アプリ追加申請を見る` で移動できるようにした。説明文は「まだ利用していないアプリについて、追加利用や相談を申請できます。」に統一する。

PR #22 main反映後の人間ブラウザ確認で、v1.3aの機能確認はOK。DB保存なしのUI mockとして完了扱いにする。

## v1.3aで実装した範囲
- `account.html` に「アプリ追加申請」エリアを追加。
- `account.html` のheroから「アプリ追加申請を見る」で申請エリアへ移動できる。
- 申請エリアを強調カードにし、利用中アプリ一覧の直後に表示。
- 利用中アプリ一覧の結果をもとに、固定候補カタログから未利用アプリを表示。
- アプリ候補ごとに `利用中` / `未利用` / `申請済み` を表示。
- 未利用アプリの「追加を申請する」ボタンから申請フォームを表示。
- 申請フォームに以下を表示。
  - 申請したいアプリ
  - 利用目的・相談内容
  - 連絡希望
- 送信後、画面内状態だけで申請済みに切り替える。
- mock modeでも申請操作の見た目を確認できる。

## 保存方針
v1.3aでは申請内容をDBへ保存しない。

保存しないもの:
- `app_instances`
- `app_data`
- `company_accounts`
- `plan_status`
- Supabase Auth
- 外部通知
- メール送信
- 決済・契約状態

申請内容はブラウザ上の一時状態だけで保持する。再読み込みすると消える。

## DB保存をしない理由
既存DB構造には、アプリ追加申請を正式に保存するための専用テーブルがない。

`app_instances` は利用中アプリを表すテーブルであり、申請中の相談内容を保存する用途には使わない。

`app_data` はアプリ内データ保存用であり、申請ワークフローの正本として使わない。

## v1.3でやらないこと
- app_instances の自動追加
- apps の本番データ変更
- app_data への申請保存
- company_accounts の契約状態変更
- plan_status 変更
- Supabase migration追加
- RLS変更
- Auth / login / signup変更
- 決済連携
- メール送信
- 管理者承認画面

## 今後の正式保存候補
正式なアプリ追加申請を扱う場合は、別Phaseで以下を設計する。

- `app_add_requests` のような申請専用テーブル
- 申請者の `company_account_id`
- 申請対象 `app_key`
- 相談内容
- 連絡希望
- 申請状態
- 作成日 / 更新日
- RLS方針
- 管理者確認方法

この設計にはDB / RLS / migrationが関わるため、実装前に人間確認が必要。

## 人間確認項目
- `account.html` で「アプリ追加申請を見る」が見える: OK
- 「アプリ追加申請を見る」から申請エリアへ移動できる: OK
- 利用中アプリ一覧の近くに「アプリ追加申請」エリアが見える: OK
- 指定説明文が表示される: OK
- 「追加を申請する」でフォームが開く: OK
- 利用目的・相談内容を入力できる: OK
- 連絡希望を選べる: OK
- 送信後に「申請を受け付けました。内容を確認後、運営からご連絡します。」と表示される: OK
- 対象アプリが `申請済み` 表示になる: OK
- 利用中アプリ一覧は壊れていない: OK
- 企業情報編集フォームは壊れていない: OK
- DB保存されていない: OK
- localStorageに申請内容を保存していない: OK
- `app_instances` / `app_data` / `company_accounts` / `plan_status` は更新されていない: OK

## PARKED
- スマホ版で「マイページ確認」の文字がヘッダーとかぶっている。
- その他UI改善余地はあるが、v1.3aの機能確認としてはブロッカーではない。

## STOP条件
- 申請内容を正式保存するために新規DBテーブルが必要。
- migration変更が必要。
- RLS変更が必要。
- app_instances の自動追加が必要。
- plan_status 変更が必要。
- 決済処理が必要。
- Auth変更が必要。
- site-config変更が必要。
- service_role / sb_secret_ / secret が必要。

## 次フェーズ候補
`v1.3b アプリ追加申請DB保存設計`

v1.3aの人間ブラウザ確認とmain反映確認は完了済み。次は正式保存に進む前に、申請専用テーブル、RLS、migration、人間確認フローを設計する。
